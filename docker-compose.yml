services:
  frontend-codex:
    build:
      context: ./easy-risk-register-frontend-codex
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ./easy-risk-register-frontend-codex:/app
      - /app/node_modules  # Prevents overwriting node_modules from the container
    environment:
      - NODE_ENV=development
    working_dir: /app
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

  frontend-qwen:
    build:
      context: ./easy-risk-register-frontend-qwen
      dockerfile: Dockerfile.dev
    ports:
      - "5174:5173"
    volumes:
      - ./easy-risk-register-frontend-qwen:/app
      - /app/node_modules  # Prevents overwriting node_modules from the container
    environment:
      - NODE_ENV=development
    working_dir: /app
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

  # Optional: If you decide to add backend services in the future
  # backend:
  #   build:
  #     context: ./easy-risk-register-backend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=development