# Development Dockerfile for hot reloading during development
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (including devDependencies) for development
RUN npm install

# Copy the rest of the application code
COPY . .

# Expose port 5173 which is the default Vite development server port
EXPOSE 5173

# Start the Vite development server with host binding to allow external connections
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]